import { Box, Text } from "@chakra-ui/react";

export const Logo = () => {
   return (
      <Box pr={4}>
         <Text fontSize="lg" fontWeight="bold" color="blue.600">
            Task Flow
         </Text>
      </Box>
   );
};
